spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pro_order
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  spring:
    security:
      enabled: false

  application:
    name: inventory-service

server:
  port: 9093
  servlet:
    context-path: /inventory-service

application:
  config:
    trace: true
